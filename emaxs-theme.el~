; +-----------------+
; | [Maxmacs Theme] |
; +-----------------+

(deftheme emaxs "Created 2024-05-27.")

; ------------------------
;   Settings up Colours
; ------------------------
;

; ----------------------
;     Text rendering
; ----------------------

(set-face-attribute 'default nil :height 200)  ; Using Ubuntu Mono rn, with 200px size
(set-face-attribute 'default nil :shadow '(5.0, "#000044", 0 . 0))

